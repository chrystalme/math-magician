const calculate = (
  calculatorData = {
    total: 0, next: 0, operation: undefined,
  },
  buttonName,
) => {
  switch (buttonName) {
    case '+':

      break;

    default:
      break;
  }
};

export default calculate;
